# AI Daily Tasks - Система утренних планов команды

Система для автоматического сбора утренних планов от команды через Telegram бот с последующей генерацией сводки через Gemini AI.

## 🎯 Что делает система

1. **Админ добавляет пользователей** через веб-панель по их @username в Telegram
2. **Пользователи подключаются** к боту, написав ему `/start` (автоматическая верификация)
3. **В 9:00 (UTC+6)** бот отправляет всем активным подключенным пользователям вопрос: "Какие задачи планируете решать сегодня?"
4. **Система ждет ответов** ровно 5 минут
5. **Через 5 минут** Gemini автоматически создает сводку всех полученных планов
6. **Админ получает** краткий отчет с планами команды и информацией о том, кто не ответил

## 💡 Новая система добавления пользователей

Теперь пользователи добавляются по **@username**, а не по User ID:

1. **Админ добавляет пользователя** по @username через веб-панель
2. **Пользователь пишет боту** `/start` для активации аккаунта
3. **Система автоматически связывает** @username с Telegram User ID
4. **Пользователь получает** утренние сообщения и может отвечать на них

### Преимущества новой системы:
- ✅ Не нужно искать сложные User ID
- ✅ Достаточно знать @username сотрудника
- ✅ Автоматическая верификация при первом контакте
- ✅ Защита от случайных пользователей

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения
Создайте файл `.env` в корне проекта:
```env
GEMINI_API_KEY=your_gemini_api_key_here
TG_BOT_TOKEN=your_telegram_bot_token_here
ADMIN_ID=your_telegram_user_id_here
```

### 3. Инициализация базы данных
```bash
python migrations/init_users.py
```

### 4. Запуск админ панели
```bash
streamlit run admin_panel/dashboard.py
```
Откройте http://localhost:8501 и добавьте пользователей по их @username

### 5. Активация пользователей
Попросите добавленных пользователей написать боту `/start` для активации

### 6. Запуск основного приложения
```bash
python -m app.main
```

## 📋 Компоненты системы

### 🔧 Админ панель (Streamlit)
- **Добавление пользователей** по @username
- **Просмотр статуса верификации** (подключен ли к боту)
- **Управление статусом** (активировать/деактивировать)
- **Просмотр ответов** пользователей
- **Статистика** команды

### 🤖 Telegram бот
- **Автоматическая верификация** при команде `/start`
- **Автоматическая рассылка** утренних вопросов только верифицированным пользователям
- **Сбор ответов** от пользователей
- **Уведомления** о статусе

### 🧠 AI сводка (Gemini)
- **Анализ планов** всей команды
- **Структурированный отчет** для руководителя
- **Автоматическая отправка** админу

### ⏰ Планировщик
- **Точное время** рассылки (9:00 UTC+6)
- **Отправка только верифицированным** пользователям
- **Контроль ответов** пользователей
- **Гибкая логика** ожидания

## 📁 Структура проекта

```
AI_daily_tasks/
├── admin_panel/
│   └── dashboard.py          # Веб-интерфейс для админа
├── app/
│   ├── api/endpoints/
│   │   └── users.py          # API для управления пользователями
│   ├── core/
│   │   ├── database.py       # Настройки БД
│   │   └── telegram_bot.py   # Telegram бот
│   ├── models/
│   │   └── user.py           # Модель пользователя
│   ├── services/
│   │   ├── bot_service.py    # Логика обработки сообщений
│   │   ├── gemini_service.py # Интеграция с Gemini AI
│   │   └── scheduler.py      # Планировщик задач
│   ├── config.py             # Конфигурация
│   └── main.py               # Главный файл приложения
├── migrations/
│   └── init_users.py         # Инициализация БД
└── requirements.txt          # Зависимости
```

## 🔐 Настройка

### Получение Gemini API ключа
1. Перейдите на https://makersuite.google.com/app/apikey
2. Создайте новый API ключ
3. Добавьте его в `.env` как `GEMINI_API_KEY`

### Создание Telegram бота
1. Напишите @BotFather в Telegram
2. Используйте команду `/newbot`
3. Получите токен и добавьте в `.env` как `TG_BOT_TOKEN`

### Получение Admin ID
1. Напишите @userinfobot в Telegram
2. Получите ваш User ID
3. Добавьте его в `.env` как `ADMIN_ID`

## 🕐 Как работает расписание

- **9:00 (UTC+6)** - Автоматическая рассылка вопросов всем активным **верифицированным** пользователям
- **9:00-9:05** - Сбор ответов от пользователей (ровно 5 минут)
- **9:05 (UTC+6)** - Автоматическая генерация и отправка сводки админу (независимо от количества ответов)

## 📊 Возможности админ панели

### Вкладка "Пользователи"
- Просмотр всех добавленных пользователей
- **Статус верификации** (подключен ли к боту)
- Статус ответов на сегодня
- Управление активностью
- Удаление пользователей

### Вкладка "Добавить"
- Добавление новых пользователей по **@username**
- Валидация username (только латиница, цифры, подчеркивания)
- Автоматическое удаление символа @ если введен

### Вкладка "Статистика"
- Общая статистика команды
- **Количество подключенных к боту**
- График регистраций
- Метрики активности

## 🔄 Процесс верификации пользователей

1. **Админ добавляет** пользователя по @username (статус: "Не активирован")
2. **Пользователь пишет** боту `/start`
3. **Система автоматически**:
   - Связывает @username с User ID
   - Устанавливает статус "Подключен к боту"
   - Активирует получение утренних сообщений
4. **Пользователь готов** получать утренние вопросы

## 🔍 Логи и мониторинг

Система ведет подробные логи всех операций:
- Верификация новых пользователей
- Отправка сообщений пользователям
- Получение ответов
- Генерация сводок через Gemini
- Ошибки и предупреждения

## ⚠️ Важные замечания

1. **Часовой пояс**: Время настроено на UTC+6 (Алматы/Астана)
2. **База данных**: Используется SQLite (файл `data/reports_backup.sqlite`)
3. **Верификация пользователей**: Пользователи должны написать боту `/start` после добавления админом
4. **Рассылка**: Утренние сообщения отправляются только верифицированным пользователям
5. **Gemini лимиты**: Учитывайте лимиты API при большом количестве пользователей

## 🆘 Решение проблем

### Пользователь не получает сообщения
- Проверьте, добавлен ли пользователь через админ панель
- Убедитесь, что пользователь написал боту `/start` (статус "Подключен к боту")
- Проверьте статус "активен" в админ панели

### Пользователь не может подключиться
- Проверьте правильность @username в админ панели
- Убедитесь, что username не изменился у пользователя
- Попросите пользователя написать боту `/start` еще раз

### Сводка не приходит админу
- Проверьте правильность `ADMIN_ID` в `.env`
- Убедитесь, что админ написал боту `/start`
- Проверьте логи на ошибки Gemini API

### Бот не запускается
- Проверьте корректность `TG_BOT_TOKEN`
- Убедитесь, что бот не заблокирован
- Проверьте доступность интернета

## 📝 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов. 